//Practice Question 08::::
/* if it's sunday & snowing -> true
if it's monday or it's raining -> true
if a number is greater than 9 & less than 100 -> true (two digit number)*/

# include <stdio.h>
# include <math.h>

int main() {
    int isSunday = 1;
    int isSnowing = 1;
    printf("%d \n", isSunday && isSnowing);
    return 0;

}